# 复制为 .env 后修改（docker-compose 会自动读取）
# cp .env.example .env

# MySQL root 密码（生产环境务必改掉）
DB_PASSWORD=your-strong-db-password

# 数据库名
DB_NAME=person_note_blog

# JWT 签名密钥（生产环境务必改成一串随机字符）
JWT_SECRET=change-me-to-a-random-string
